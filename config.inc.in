# force CC to gcc incase the default compilers are different
CC=@GCC@
CXX=@GXX@

# where is python?
PYTHON=@PYTHON_PATH@

# Do we build the documentation?
# this doesn't work on a lot of Linux distros
# due to docbook incompatibilities

SI_BUILD_DOCS=@BUILD_DOCS@


