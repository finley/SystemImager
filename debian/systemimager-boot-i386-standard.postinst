#!/bin/sh
if [ -e /tftpboot/kernel -o -e /tftpboot/initrd.img ]; then
	echo
	echo "WARNING: default tftpboot directory has been moved to /var/lib/tftpboot!"
        echo "If you don't like it, change /etc/systemimager/systemimager.conf"
	echo
fi

#DEBHELPER#
